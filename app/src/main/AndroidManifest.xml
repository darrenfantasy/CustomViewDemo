<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.fantasy.darren.mycanvasdemo"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>
    <protected-broadcast android:name="android.intent.action.MEDIA_MOUNTED"></protected-broadcast>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.AnimationButtonActivity"/>
        <activity android:name=".ui.MyCanvasActivity"/>
        <activity android:name=".ui.MySwitchButtonActivity"/>
        <activity android:name=".ui.MyPickerViewActivity"/>
    </application>

</manifest>